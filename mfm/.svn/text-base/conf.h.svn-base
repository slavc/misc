#ifndef CONF_H
#define CONF_H

struct conf {
	int window_x;
	int window_y;
	int window_w;
	int window_h;
};

void conf_defaults(struct conf *pc);
int load_conf(struct conf *pc);
int save_conf(struct conf *pc);

#endif /* CONF_H */
